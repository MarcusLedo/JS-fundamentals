const nome = "Rebecca";
const conca = "Olá " + nome + "!";
const template = `
    Olá 
    ${nome}!`;

    console.log(conca + "\n" + template);

    console.log(`1 + 1 =  ${1 + 1}`);

const up = texto => texto.toUpperCase();  //função?

    console.log(`Ei... ${up("cuidado")}`);